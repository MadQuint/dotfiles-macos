- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    # ~/.vim: vim

- create:
    - ~/Work

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]